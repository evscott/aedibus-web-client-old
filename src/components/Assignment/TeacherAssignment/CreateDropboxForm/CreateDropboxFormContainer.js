import { connect } from 'react-redux';
import Assignment from "./CreateDropboxForm";

const mapStateToProps = (state) => {
    return {};
};

const mapDispatchToProps = (dispatch) => {
    return {};
};

const CreateDropboxFormContainer = connect(
    mapStateToProps,
    mapDispatchToProps
)(Assignment);

export default CreateDropboxFormContainer;